import React from 'react';

const Courses = () => {
    return (
        <div style={{ paddingTop: '180px', minHeight: '100vh', textAlign: 'center' }} className="container">
            <h1 className="section-title">All Courses</h1>
            <p className="section-desc">Course catalog content will go here.</p>
        </div>
    );
};

export default Courses;